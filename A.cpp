#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define ar array

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	int n, w;
	cin >> n >> w;
  //just divide the total weight that the truck can carry by the weight of each brick
	cout << n/w << "\n";
}
